# Frontend

The complete ERP frontend UI lives on the **`ui`** branch.

```bash
git checkout ui
cd frontend
npm install
npm run dev
```

Stack: React + TypeScript + Vite, Tailwind CSS, Storybook, npm workspaces (`apps/erp`, `packages/ui`).
