/**
 * `@erp/ui/fixtures` -- every sample dataset the package ships, in one
 * place. Components keep their fixtures beside them; this is only the
 * subpath's entry, so a component adding fixtures adds one line here.
 */
export * from "../components/BusinessDocument/fixtures";
