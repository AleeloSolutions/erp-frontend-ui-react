/**
 * Shared loading/error helpers for inventory server lists.
 *
 * Re-exported from `@/lib/listTableState` so Sales and Inventory share one
 * definition of cold `loading` vs soft `fetching`.
 */

export { listTableState } from "@/lib/listTableState";
